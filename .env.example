# 환경변수 예시 — 복사 후 .env로 이름 변경하여 사용
# cp .env.example .env

# PostgreSQL 연결
DATABASE_URL=postgresql://wine:wine@localhost:5432/winedeals

# Redis 연결
REDIS_URL=redis://localhost:6379/0

# (선택) GS25 로그인이 필요한 경우
# GS25_ID=your_id
# GS25_PW=your_password
